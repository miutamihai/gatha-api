# import Person from '../person/type.graphql'
# import PersonParameter from '../people/type.graphql'
# import PersonInput from '../person/type.graphql'

type Query {
    people(input: PersonParameter): [Person]
}

type Mutation {
    person(input: PersonInput): Person
}
